datasource db {
    provider = "sqlite"
    url      = "file:dev.db"
}

generator client {
    provider      = "cargo prisma"
    output        = "../src/prisma.rs"
}

model Todo {
    id          Int        @id @default(autoincrement())
    title       String
    is_complete Boolean
}
